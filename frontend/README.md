# AgentForge frontend

React 19 and Vite client for AgentForge.

```powershell
npm ci
npm run dev
```

Required public environment variables are documented in `.env.example`.
Project architecture, full local setup, verification commands, and production
links are in the repository root `README.md`.
